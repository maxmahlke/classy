"""Module to test classy smoothing routines."""

# Verify that has_smoothing_parameters returns True if spectrum
# present in smoothing.csv

# Verify that savgol smoothing with fixed degree + window correctly sets
# the reflectance of a given spectrum
